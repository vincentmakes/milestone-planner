# FastAPI and ASGI
fastapi==0.141.1
# Transitive via FastAPI, pinned directly: FastAPI allows starlette>=0.46 with no
# upper bound, and this is the floor that carries the form-parser, StaticFiles and
# request-URL security fixes.
starlette==1.4.1
uvicorn[standard]==0.27.1
python-multipart==0.0.31

# Database
sqlalchemy[asyncio]==2.0.25
asyncpg==0.29.0
psycopg2-binary==2.9.9

# Authentication & Sessions
bcrypt>=4.0.0
httpx==0.26.0

# Encryption (for tenant credentials)
cryptography==50.0.0

# Validation & Serialization
pydantic==2.13.4
pydantic-settings==2.1.0
email-validator==2.3.0

# Utilities
python-dotenv==1.2.2
python-dateutil==2.8.2
orjson==3.11.6
psutil==5.9.8  # For system stats

# MPXJ for MS Project file parsing
jpype1==1.5.0
mpxj>=12.0.0  # Python wrapper with bundled MPXJ JARs

# Excel export
openpyxl==3.1.5
